# Optional CNN Artifacts

Place exported model artifacts here for optional cricket shot CNN inference:

- `cricket_shot_model.pth`
- `class_mapping.json`
- `preprocess_config.json`
- `model_config.json`

If these files are missing or invalid, backend automatically falls back to existing pose-based shot inference.
